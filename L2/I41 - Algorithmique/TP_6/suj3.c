#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>


typedef unsigned int uint;
typedef unsigned short ushort;
typedef unsigned long ullong;

void echanger(uint *L, uint i, uint j){
  int bis;
  bis = L[i];
  L[i] = L[j];
  L[j] = bis;
}

ullong Propager(int * T, uint n, uint a, uint b, ullong* e){
  e =
  if(a < b){
    for(int i = 0; i < n; i++){
      echanger(T, a, b);
    }
  }
  else{
    for(int j = sizeof(L); j > 0; j--){
      
    }
  }
  return T;
}
